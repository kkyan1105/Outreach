// TODO: Person A — Outing info card component
import { View, Text } from "react-native";

export default function OutingCard() {
  return (
    <View style={{ padding: 16, backgroundColor: "#fff", borderRadius: 12, shadowColor: "#000", shadowOpacity: 0.1, shadowRadius: 4 }}>
      <Text>Outing Card</Text>
    </View>
  );
}
