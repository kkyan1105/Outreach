// TODO: Person A — react-native-maps component (Phase 4)
import { View, Text } from "react-native";

export default function MapView() {
  return (
    <View style={{ width: "100%", height: 400, backgroundColor: "#e5e7eb", borderRadius: 12, justifyContent: "center", alignItems: "center" }}>
      <Text style={{ color: "#9ca3af" }}>Map loading...</Text>
    </View>
  );
}
